// Phase 0 stub — replaced in Phase 3 with the real server bootstrap.
// Intentionally minimal: just enough to verify Node.js native TS execution works.
process.stdout.write('mcp-server: phase 0 stub\n');
